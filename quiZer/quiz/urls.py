from django.urls import path
from . import views

urlpatterns = [
    #path('quiz/add/', views.add_quiz, name='add_quiz'),
    #path('quiz/edit/<str:title>', views.edit_quiz, name='edit_quiz'),
    #path('quiz/<str:slug>/', views.view_quiz, name='view_quiz'),
]
